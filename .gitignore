node_modules/
.wrangler/
.dev.vars
dist/
coverage/
.DS_Store

# scripts/import-spots.ts resumable-run progress (scripts/lib/progress.ts) — a real crawl over ~8032
# slugs churns this file every chunk; it must never sit untracked next to tracked docs in .superpowers/.
.superpowers/import-spots-progress.json
*.progress.json

# npm run compare:sf — the CSV piles up locally day after day, and the saved pages serve to debug the parser
data/sf-compare.csv
tmp/

# your own Cloudflare config: same as wrangler.toml, with your KV namespace id
wrangler.local.toml
